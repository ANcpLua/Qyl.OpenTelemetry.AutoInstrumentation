# GOLDEN — M1 walking skeleton — HttpClient CLIENT span
# Normalizer: drop volatile fields (TraceId, SpanId, ParentSpanId, timestamps, duration)
# AND outcome-dependent fields (http.response.status_code, error.type) — these vary with
# network state and must NOT be asserted, or the gate becomes flaky.
# Assert the stable, request-time field set below is present on exactly ONE exported span.
# Negative control: WITHOUT attach, span count MUST be 0.

Activity.DisplayName  = GET
Activity.Kind         = Client
http.request.method   = GET
server.address        = example.com
url.full              = https://example.com/

# Gate B reference (app-observable, must be identical with/without attach, network-independent):
#   APP_DONE
#   exit=0
#
# NOTED non-failure: with attach, the outbound request gains a W3C `traceparent` header — a
# wire-level change that is intentionally INVISIBLE to the app, so NOT a Gate B violation.
# Flag for T015/T016/T019 (http/rpc/messaging) where propagation is the point.
