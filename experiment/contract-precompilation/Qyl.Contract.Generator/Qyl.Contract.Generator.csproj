<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <IsRoslynComponent>true</IsRoslynComponent>
    <EnforceExtendedAnalyzerRules>true</EnforceExtendedAnalyzerRules>
    <!-- RSEXPERIMENTAL007: the experimental pre-compilation API. RS2008: release tracking (irrelevant here). -->
    <NoWarn>$(NoWarn);RSEXPERIMENTAL007;RS2008</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.CodeAnalysis.CSharp" Version="5.9.0-1.26324.7" PrivateAssets="all" />
  </ItemGroup>

</Project>
